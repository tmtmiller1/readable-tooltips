{
  "name": "readable-tooltips",
  "version": "1.0.4",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "npx --yes eslint \"ui/**/*.js\" --no-error-on-unmatched-pattern",
    "check": "node --check ui/*.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run lint && npm run check && npm run test"
  }
}
